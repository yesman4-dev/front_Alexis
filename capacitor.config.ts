import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hatsumi.qonda',
  appName: 'QondaApp',
    webDir: 'dist/reservaciones'
};

export default config;
